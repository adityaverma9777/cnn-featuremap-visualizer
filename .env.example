# Backend runtime settings
HOST=0.0.0.0
PORT=8000
WORKERS=1

# Comma-separated allowed origins for browser calls (frontend domains)
# Example:
# CORS_ALLOW_ORIGINS=https://your-frontend.onrender.com,https://yourdomain.com
CORS_ALLOW_ORIGINS=*
